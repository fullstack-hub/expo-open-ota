name: FIET EKS 배포
on: {push: {branches: [main]}, workflow_dispatch: {}}
jobs:
  deploy:
    permissions: {contents: write, id-token: write}
    uses: fullstack-hub/fullstackhub-devops/.github/workflows/fiet-eks-build.yml@main
    with:
      ecr_repository: apps/expo-open-ota
      docker_context: .
      dockerfile: Dockerfile
      values_file: helm/expo-open-ota/values-fiet.yaml
    secrets: inherit
